матрешка изолированный dom внутри dom, который создается js'ом и может содержать теги dom.
помечается в html как shadow-root

боль для автотестов, т.к. selenium  не может взаимодействовать с эго элементами

[[DOM]]